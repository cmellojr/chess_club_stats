<context path="docs" updated="2026-06-09">

Project documentation. Contains user-facing guides and reference material
in markdown format, maintained alongside the code.

## Summary

- `usage.md` — Installation, authentication setup (OAuth PKCE vs session cookies), library integration, output formatting, verbose flag
- `features.md` — Auto-maintained feature map: every user-facing feature and the code path (file:line) that implements it
- `cli-commands.md` — Complete CLI command reference with blueprints, subcommand structures, output formats, pagination behavior, Swiss leaderboard fallbacks
- `cache.md` — SQLite cache architecture: schema, WAL journal mode, cache key serialization, per-URL TTL policies
- `roadmap.md` — Development roadmap with completed phases, architecture decision records (ADRs), and known issues

</context>
